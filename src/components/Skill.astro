---

---

<dl>
	<slot />
</dl>

<style>
	dl {
		align-items: baseline;
		gap: theme('spacing.6');
		line-height: 1.6;
		position: relative;

		@container (width >= 40rem) {
			display: grid;
			grid-template-columns: auto 1fr;
		}
	}
</style>
