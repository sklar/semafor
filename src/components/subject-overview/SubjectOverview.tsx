import type { Topic } from '@/lib/topics'

interface SubjectOverviewProps {
	topics: Topic[]
	subject: string
}

export default function SubjectOverview(_props: SubjectOverviewProps) {
	return <div />
}
